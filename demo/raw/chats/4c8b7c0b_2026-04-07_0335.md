# Chat — 2026-04-07 03:35 UTC
Session: 4c8b7c0b-9dcd-4b20-a523-b3c2fd1a6bee

<details>
<summary>Ran 2 commands, Found 1 pattern, Read 5 files</summary>

- **Bash** — ls projects/
- **Glob** — projects/**/README.md
  - projects/README.md
projects/transformer-from-scratch/README.md
projects/attention-efficiency-paper/README.md
projects/data-pipeline/README.md
projects/research-journal/README.md
projects/future-project/README.md
- **Read** — projects/transformer-from-scratch/README.md
- **Bash** — ls -R projects/transformer-from-scratch/
  - 1	---
2	tags:
3	- code
4	- transformers
5	title: Transformer From Scratch
6	type: project
7	---
8	
9	# Transformer From Scratch
10	
11	Minimal PyTorch implementation of the [[Transformer Architecture]].
12	
13	## Files
14	- `attention.py` — [[Self-Attention]] and [[Multi-Head Attention]]
15	- `model.py` — full transformer with encoder blocks
16	- `train.py` — training loop on synthetic data
17	- `config.yaml` — hyperparameters
18	
19	## Status
20	Encoder working. Next: decoder + cross-attention.
21	
22	## Uses
23	- [[Attention Mechanisms]]
24	- [[Transformer Architecture]]
25	
- **Read** — projects/transformer-from-scratch/config.yaml
  - 1	model:
2	  d_model: 256
3	  n_heads: 4
4	  n_layers: 4
5	  d_ff: 512
6	  vocab_size: 1000
7	
8	training:
9	  batch_size: 8
10	  seq_length: 32
11	  learning_rate: 0.0003
12	  steps: 1000
13	
- **Read** — projects/transformer-from-scratch/src/attention.py
  - 1	import torch
2	import torch.nn as nn
3	import math
4	
5	class SelfAttention(nn.Module):
6	    def __init__(self, d_model, n_heads):
7	        super().__init__()
8	        self.n_heads = n_heads
9	        self.d_k = d_model // n_heads
10	        self.W_q = nn.Linear(d_model, d_model)
11	        self.W_k = nn.Linear(d_model, d_model)
12	        self.W_v = nn.Linear(d_model, d_model)
13	        self.W_o = nn.Linear(d_model, d_model)
14	
15	    def forward(self, x):
16	        B, T, C = x.shape
17	        q = self.W_q(x).view(B, T, self.n_heads, self.d_k).transpose(1, 2)
18	        k = self.W_k(x).view(B, T, self.n_heads, self.d_k).transpose(1, 2)
19	        v = self.W_v(x).view(B, T, self.n_heads, self.d_k).transpose(1, 2)
20	        attn = (q @ k.transpose(-2, -1)) / math.sqrt(self.d_k)
21	        attn = torch.softmax(attn, dim=-1)
22	        out = (attn @ v).transpose(1, 2).contiguous().view(B, T, C)
23	        return self.W_o(out)
24	
- **Read** — projects/transformer-from-scratch/src/model.py
  - 1	import torch.nn as nn
2	from attention import SelfAttention
3	
4	class TransformerBlock(nn.Module):
5	    def __init__(self, d_model, n_heads, d_ff):
6	        super().__init__()
7	        self.attention = SelfAttention(d_model, n_heads)
8	        self.norm1 = nn.LayerNorm(d_model)
9	        self.ff = nn.Sequential(nn.Linear(d_model, d_ff), nn.GELU(), nn.Linear(d_ff, d_model))
10	        self.norm2 = nn.LayerNorm(d_model)
11	
12	    def forward(self, x):
13	        x = x + self.attention(self.norm1(x))
14	        x = x + self.ff(self.norm2(x))
15	        return x
16	
- **Read** — projects/transformer-from-scratch/src/train.py
  - 1	import torch
2	from model import Transformer
3	
4	model = Transformer(vocab_size=1000)
5	opt = torch.optim.Adam(model.parameters(), lr=3e-4)
6	
7	for step in range(100):
8	    x = torch.randint(0, 1000, (8, 32))
9	    loss = torch.nn.functional.cross_entropy(model(x).view(-1, 1000), x.view(-1))
10	    opt.zero_grad(); loss.backward(); opt.step()
11	    if step % 10 == 0: print(f"Step {step}, Loss: {loss.item():.4f}")
12	

</details>
